
__pycache__/
.build/
.cache.json
.DS_Store
.seed-version
.seen.json
.tracked.json
*.csv
*.dmg
*.pyc
# and the DMG is a couple of megabytes, so neither belongs in the history.
# Build artifacts
# Generated output
# macOS
# make-app.sh drops these in the checkout; both are rebuildable
# Personal, and nothing anyone else should get a copy of.
# Written by the app to know which bundled roster it last merged.
# Your own state — saved/applied roles, seen history, cached results.
internships.md
new.md
QuantJobs.app/
QuantJobsApp/Icon/QuantJobs.icns
QuantJobsApp/Icon/QuantJobs.iconset/
roles.md
Tools/.ax
Tools/.window-id
web/
